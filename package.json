{
  "name": "inijmegen",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply:local": "wrangler d1 execute inijmegen-cms --local --file=./schema/001_init.sql",
    "db:apply:remote": "wrangler d1 execute inijmegen-cms --remote --file=./schema/001_init.sql",
    "seed:local": "wrangler d1 execute inijmegen-cms --local --file=./schema/002_seed.sql",
    "seed:remote": "wrangler d1 execute inijmegen-cms --remote --file=./schema/002_seed.sql"
  },
  "dependencies": {
    "hono": "^4.12.31",
    "marked": "^18.0.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^6.0.3",
    "wrangler": "^4.114.0"
  }
}
